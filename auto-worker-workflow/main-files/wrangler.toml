name = "workflow-slave"
main = "worker.js"
compatibility_date = "2025-01-01"
upload_format = "modules"
compatibility_flags = ["nodejs_compat"]

[vars]
GITHUB_OWNER = "kbence2000"
GITHUB_REPO = "ASA_FULL"
BASE_BRANCH = "main"

# These are documented only; actual values must be added in Cloudflare Dashboard
# - GITHUB_TOKEN
# - OPENAI_API_KEY
# - CLOUDFLARE_API_TOKEN
# - ASA_AGENT_ENDPOINT
# - ASA_MATRIX_HOOK

[build]
command = ""


README.txt
# workflow-slave – ASA MATRIX Harmonizer Worker

This Worker provides:
- /api/diff
- /api/harmonize
- /api/apply
- /api/status

## Environment variables (Cloudflare Dashboard)
- GITHUB_OWNER
- GITHUB_REPO
- BASE_BRANCH
- GITHUB_TOKEN (secret)
- OPENAI_API_KEY (secret)
- CLOUDFLARE_API_TOKEN (secret)
- ASA_AGENT_ENDPOINT (secret)
- ASA_MATRIX_HOOK (optional)

## Deploy
Connected via GitHub integration.
Worker deploys automatically on push.
